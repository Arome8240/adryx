{
  "name": "adryx",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "dev": "turbo dev",
    "dev:frontend": "turbo dev --filter=adryx-frontend",
    "dev:backend": "turbo dev --filter=adryx-backend",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "release": "turbo build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "prettier": "^3.2.5",
    "turbo": "latest",
    "typescript": "^5.4.2"
  }
}
